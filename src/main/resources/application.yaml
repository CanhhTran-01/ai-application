server:
  port: 8080

spring:
  application:
    name: ai-application
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

  ai:
    model:
      chat: google-genai

    google:
      genai:
        api-key: ${GOOGLE_API_KEY}
        chat:
          model: gemini-3.6-flash